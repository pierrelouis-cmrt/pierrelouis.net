{
  "devDependencies": {
    "markdown-it": "^14.1.0",
    "markdown-it-abbr": "^2.0.0",
    "markdown-it-container": "^4.0.0",
    "markdown-it-deflist": "^3.0.0",
    "markdown-it-footnote": "^4.0.0",
    "markdown-it-mark": "^4.0.0",
    "markdown-it-mathjax": "^2.0.0",
    "markdown-it-task-lists": "^2.1.1",
    "nodemon": "^3.1.10",
    "tailwindcss": "^3.4.14"
  },
  "scripts": {
    "build:css": "node scripts/buildCssVersioned.mjs",
    "build:posts": "node scripts/buildPosts.js",
    "build:bookmarks": "node scripts/buildBookmarks.js",
    "build:md": "node scripts/buildMdPages.js",
    "build:push": "node scripts/pushBuildBranch.mjs",
    "build": "npm run build:css && npm run build:posts && npm run build:bookmarks && npm run build:md && npm run build:push",
    "build:content": "npm run build:posts && npm run build:bookmarks && npm run build:md",
    "dev": "node scripts/buildCssVersioned.mjs --dev && node scripts/buildPosts.js && node scripts/buildMdPages.js && npx tailwindcss -i ./src/styles.css -o ./src/output-dev.css --watch"
  }
}
